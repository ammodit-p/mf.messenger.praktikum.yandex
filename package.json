{
    "name": "mf.messenger.praktikum.yandex",
    "version": "1.0.0",
    "private": true,
    "devDependencies": {
        "@types/chai": "^4.2.15",
        "@types/handlebars": "^4.1.0",
        "@types/mocha": "^8.2.1",
        "chai": "^4.3.0",
        "copyfiles": "^2.4.1",
        "express": "4.17.1",
        "handlebars": "^4.7.6",
        "mocha": "^8.3.0",
        "sass": "^1.32.8",
        "ts-node": "^9.1.1"
    },
    "scripts": {
        "tsc": "tsc",
        "start": "node ./server.js",
        "dev": "tsc -w & npm run html & npm run css-w & npm run start & browser-sync start --proxy 'localhost:4000' --files 'dist'",
        "clean": "rm -rf dist/",
        "fix-js": "find ./dist -name \"*.js\" -exec sed -i '' -E 's|(import .+ from ['\\''\"]\\.?\\./.+[^.][^j][^s])(['\\''\"])|\\1.js\\2|g' {} +",
        "html": "find ./src/ -name \"*.html\" -type f -exec copyfiles -u 1 {} ./dist \\;",
        "css": "sass src/:dist/",
        "css-w": "sass --watch src/:dist/",
        "build": "npm run clean && npm run tsc && npm run html && npm run css",
        "test": "mocha -r ts-node/register src/**/*.spec.ts",
        "compile": "babel src -d lib --extensions '.ts,.tsx'"
    },
    "dependencies": {
        "@types/handlebars": "^4.1.0",
        "copyfiles": "^2.4.1",
        "handlebars": "^4.7.6",
        "typescript": "4.1.2"
    },
    "engines": {
        "node": "12.16.0",
        "npm": "6.13.4"
    }
}

import {Block} from "./classBlock.js";

export class Button extends Block {
    constructor(props: {}, tmpl: string) {
      super("button", props, tmpl);
    }
  
// render перенесла отсюда в класс блок
  }

  